{
  "name": "medidiag",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix medidiag-hospital-app/hospital-app run build",
    "start": "npm --prefix medidiag-hospital-app/hospital-app run preview -- --host 0.0.0.0 --port $PORT",
    "dev": "npm --prefix medidiag-hospital-app/hospital-app run dev -- --host 0.0.0.0 --port 5173"
  }
}
